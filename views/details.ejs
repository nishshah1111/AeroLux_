<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= plane.manufacturer %> <%= plane.model %> | AeroLux</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Playfair+Display:wght@700;800&display=swap" rel="stylesheet">
    <script src="https://aframe.io/releases/1.3.0/aframe.min.js"></script>
</head>
<body class="azure-sky-bg platinum-grey-text">

    <header class="silver-lining-bg sticky top-0 z-50 shadow-md">
        <%- include('./partials/header') %>
    </header>

    <main class="container mx-auto px-6 py-16">
        <div class="mb-12">
            <h1 class="text-4xl md:text-5xl font-bold"><%= plane.manufacturer %> <%= plane.model %></h1>
            <p class="text-lg"><%= plane.description %></p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-5 gap-8">
            <div class="lg:col-span-3">
                <img id="main-jet-image" src="<%= plane.image_url %>" alt="<%= plane.manufacturer %> Exterior" class="w-full h-auto object-cover rounded-lg shadow-lg mb-4">
                </div>
            <div class="lg:col-span-2">
                <div class="bg-white p-6 rounded-lg shadow-lg">
                    <h2 class="text-2xl font-bold mb-4">Key Information</h2>
                    <div class="space-y-3">
                        <div class="flex justify-between"><span>Price:</span> <span class="font-bold text-lg sunlit-gold-text">$<%= plane.price.toLocaleString('en-US') %></span></div>
                        <div class="flex justify-between border-t pt-2"><span>Manufacturer:</span> <span class="font-semibold"><%= plane.manufacturer %></span></div>
                        <div class="flex justify-between border-t pt-2"><span>Model:</span> <span class="font-semibold"><%= plane.model %></span></div>
                        <div class="flex justify-between border-t pt-2"><span>Year:</span> <span class="font-semibold"><%= plane.year %></span></div>
                        <div class="flex justify-between border-t pt-2"><span>Serial Number:</span> <span class="font-semibold"><%= plane.serial_number %></span></div>
                        <div class="flex justify-between border-t pt-2"><span>Total Time (TTAF):</span> <span class="font-semibold"><%= plane.ttaf %> Hours</span></div>
                        <div class="flex justify-between border-t pt-2"><span>Location:</span> <span class="font-semibold"><%= plane.location %></span></div>
                    </div>
                    <a href="/contact" class="mt-6 block w-full text-center px-6 py-3 rounded-md sunlit-gold-accent text-white hover:opacity-90 transition-opacity">Inquire About This Jet</a>
                </div>
            </div>
        </div>

        <div class="mt-16 bg-white p-8 rounded-lg shadow-lg">
            <h2 class="text-3xl font-bold mb-6 text-center">Detailed Specifications</h2>
            <div class="space-y-8">
                <div>
                    <h3 class="text-2xl font-bold mb-3 border-b-2 sunlit-gold-border pb-2">Engines</h3>
                    <p>Model: <%= plane.engine_model %></p>
                </div>
                <div>
                    <h3 class="text-2xl font-bold mb-3 border-b-2 sunlit-gold-border pb-2">Avionics</h3>
                    <p><%= plane.avionics_suite %></p>
                </div>
                 <div>
                    <h3 class="text-2xl font-bold mb-3 border-b-2 sunlit-gold-border pb-2">Interior</h3>
                    <p><%= plane.interior_desc %></p>
                </div>
            </div>
        </div>
        <section id="inquiry-form" class="mt-16 silver-lining-bg p-8 rounded-lg shadow-lg max-w-4xl mx-auto">
    <h2 class="text-3xl font-bold mb-6 text-center">Contact Us About the <%= plane.manufacturer %> <%= plane.model %></h2>
    <form action="/inquire" method="POST">
        <input type="hidden" name="aircraftId" value="<%= plane.id %>">
        <input type="hidden" name="subject" value="Inquiry about <%= plane.manufacturer %> <%= plane.model %> (ID: <%= plane.id %>)">

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <input type="text" name="userName" placeholder="Your Name" required class="w-full p-3 rounded-md">
            <input type="email" name="userEmail" placeholder="Your Email" required class="w-full p-3 rounded-md">
        </div>
        <textarea name="message" placeholder="Your Message..." rows="5" required class="w-full p-3 rounded-md mt-6"></textarea>
        <button type="submit" class="mt-6 w-full p-3 rounded-md sunlit-gold-accent text-white font-semibold hover:opacity-90">Send Inquiry</button>
    </form>
</section>
    </main>


    <%- include('./partials/footer') %>

    <script src="/script.js"></script>
</body>
</html>